#!/usr/bin/env bash
# Utility to regenerate placeholder SVGs (not needed at runtime).
# The committed .svg files already exist - this script is just for reference.
echo "Placeholders are in this folder as .svg files. Replace them with your own images"
echo "(same filename, any format works if you update the <img src=''> in index.html)."
